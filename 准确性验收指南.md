# 准确性改造验收指南

> 适用项目：`C:\Users\m1738\Desktop\小说`
> 验收对象：参考资产 → 关键帧（img2img）→ 图生视频（I2V）→ 字幕/文字叠加 → 片段合成
> 验收方式：你自己操作 + 一条只读检查命令

---

## 0. 验收前必须确认的三件事

1. 远程 ComfyUI 可访问（`.env` 中 `COMFYUI_BASE_URL`，默认 `http://10.7.89.34:8188`）。
2. `.env` 中以下开关处于打开状态：

```text
MEDIA_PROVIDER=comfyui
COMFYUI_KEYFRAME_WORKFLOW_PATH=./workflows/krea2-keyframe-img2img-api.json
COMFYUI_KEYFRAME_WORKFLOW_MANIFEST_PATH=./workflows/manifests/krea2-keyframe-img2img.json
COMFYUI_KEYFRAME_USE_REFERENCE=true
KEYFRAME_REQUIRED=true
COMPOSER_PROVIDER=ffmpeg
FFMPEG_FONT_PATH=C:/Windows/Fonts/msyh.ttc
```

3. 启动服务：`pnpm dev`，打开 `http://127.0.0.1:4173`，进入《夜行者》项目。

> 注意：.env 里现在有 3 个同名“夜行者”。请选择带前期策划和视频状态的那个（数据库 ID `dd3c8fc7-f60b-4f1f-9c2b-91042d84d597`）。

---

## 0.5 快速验收模式（强烈建议先跑这个）

正式链路一次会生成 9～12 个镜头，跑一轮要十几分钟，不适合反复调准确性。生成配置页现在有一个开关：

> 快速验收模式：每个片段只生成 1 个镜头 · 约 5 秒

打开后：

- 策划阶段每个片段只保留 1 个节拍 → 分镜只有 1 个镜头；
- 该镜头时长固定压到 5 秒（不改变旁白音频本身）；
- 成片只截取旁白开头约 5 秒，字幕同步裁到同一段内容。

一轮总耗时大约：1 张关键帧候选（3 图）+ 1 次 I2V + 1 次合成 ≈ 2 分钟，用来验证"参考图 → 关键帧 → 图生视频 → 字幕/文字 → 合成"整条链路是否成立。

**注意**：这个开关在「生成配置」页，只对**新创建的策划**生效。

- 新项目：新建作品 → 粘贴正文 → 生成配置页勾选开关 → 开始前期策划。
- 已经确认过分镜的老项目（比如「夜行者2」）：不用重新改写文案，服务启动后跑一条命令即可基于现有策划生成一个验收版（会新建一个策划版本，旧版本不受影响）：

```bash
node scripts/acceptance-plan.mjs --project=夜行者2
```

跑完会打印"策划完成：1 个镜头，时长 5.0s"，然后回页面继续走关键帧 → 图生视频。

跑完快速验收、确认链路没问题之后，再关掉开关跑一次完整版本，才是真正的成片。

---

## 1. 验收步骤（前端操作）

### 步骤 1：填写角色与场景设定

位置：项目 → 前期策划审核 → **视觉资产** 标签 → 页面下方「角色与场景设定」。

1. 给「沈砚」填写：年龄、面部与五官、发型发色、身高体型、固定服装。
   这些字段就是**角色参考图提示词的唯一来源**，不填就只剩“男性”这种没有辨识度的描述；也不要再出现“具体年龄、发型与五官未交代”这类话（系统会剔除，但等于没有素材）。
2. 给「废弃地铁站站台」填写：空间描述、空间方向与出入口、光源与照明、色板。
3. 每张卡片点「保存设定」。

预期结果：

- 提示“设定已保存：N 张旧参考图已失效，请重新生成参考图并重新确认关键帧。”
- 该实体卡片状态回到「待确认」（旧参考图不再被当作有效锚点）。
- 关键帧审核标签里，9 个镜头全部回到「未生成 / 待选择」状态。

### 步骤 2：重新生成并锁定参考图

位置：视觉资产标签。

1. 对「沈砚」点「生成 4 张参考候选」，等待候选图出现。每张候选都是**角色全身三视图**（正面 / 侧面 / 背面三个全身视角并排在同一张图里，同一套发型、服装和配色）。
2. 挑一张三视图最符合你设定的，点它锁定为主参考图（它同时是后续关键帧 img2img 的身份锚点）。
3. 对「废弃地铁站站台」做同样操作。

> 注意：改了角色设定或换了一张主参考图，旧关键帧和旧视频会自动失效，必须重新生成关键帧，再重新出视频。

预期结果：

- 每张卡片显示「已锁定」，并显示选中的主参考图。
- 这一步之后才允许进入关键帧生成；没有锁定参考图时，关键帧任务会直接失败并给出 `KEYFRAME_REFERENCE_MISSING`。

### 步骤 3：逐镜生成并确认关键帧

位置：**关键帧审核** 标签。

1. 点底部「生成缺失关键帧（9）」。
2. 每个镜头出 3 张候选后，逐镜点选一张作为正式关键帧。

预期结果：

- 9 个镜头全部显示「已确认」。
- 底部按钮从「生成缺失关键帧」变为「开始图生视频」。
- 这一步是关键：视频只负责让这张已确认的画面动起来。

### 步骤 4：生成图生视频 + 合成

1. 点「开始图生视频」。
2. 等待任务完成（9 个镜头逐个生成，约 1 分钟/镜）。

预期结果：

- 片段状态变为 ready，生成页可播放成片。
- 成片时长与旁白一致（当前为 35256ms / 24fps / 576×1024）。
- 成片底部出现与旁白一致的字幕（旧版本没有字幕，本次会被重新生成并烧录）。
- 第 6 镜时间段能看到 23:17 叠加文字。

---

## 2. 自动检查（一条命令）

```bash
pnpm verify
```

等价于：

```bash
node scripts/verify-accuracy.mjs --project=dd3c8fc7-f60b-4f1f-9c2b-91042d84d597
```

脚本是只读的：只查数据库和媒体文件，不会修改任何数据。

### 应该看到的结果

| 检查项 | 期望 |
|---|---|
| 数据库 schema 版本 ≥ 8 | 通过 |
| 存在已确认的前期策划版本 | 通过 |
| 存在已确认的 Visual Bible | 通过 |
| 每个镜头都已确认关键帧 | 9/9 |
| 关键帧实际消费了参考图 | 9/9 使用参考图，profile 为 `krea2-keyframe-img2img` |
| 每镜视频都消费了已确认关键帧 | 9/9 使用首帧 |
| 镜头时长误差 ≤ 50ms | 全部在容差内 |
| 镜头时长之和等于真实 TTS 时长 | 相等 |
| 字幕已生成有效时间轴 | cueCount > 0 且 srt 存在 |
| 成片包含视频流与音轨 | 均为 true |
| 成片记录了后期文字叠加 | 至少一条 23:17 记录 |
| 成片烧录了字幕 | 已烧录 |
| 镜头写入了 mustNotShow 约束 | 9/9 |

任何一项显示「失败」都会让脚本返回非 0 退出码，说明链路没有真正闭环，需要按第 4 节的错误码处理。

---

## 3. 必须人工看的 6 件事

自动脚本只能验证数据契约和技术参数，下面这些必须你看画面：

1. 第 6 镜：23:17 是否叠加在电子屏位置上（当前按固定位置 `x=0.68w, y=0.13h` 叠加，可能压偏）。
2. 第 7 镜：画面中不得出现列车实体，只能有人物的转头/警觉反应。
3. 第 8、9 镜：不得出现女人实体，站台保持无人。
4. 人物一致性：9 镜中沈砚的脸型、发型、服装是否一致。
5. 字幕：内容与旁白一致、无乱码、位置不遮挡主体。
6. 音频：旁白存在、音画不错位。

---

## 4. 失败时的错误码对照

| 错误码 | 含义 | 处理 |
|---|---|---|
| `KEYFRAME_REFERENCE_MISSING` | 该镜头没有可用的已锁定参考图 | 回到视觉资产标签，生成并锁定主参考图 |
| `KEYFRAME_REFERENCE_NOT_USED` | 关键帧实际走了文生图，没有消费参考图 | 检查 `COMFYUI_KEYFRAME_WORKFLOW_PATH` / `..._MANIFEST_PATH` / `COMFYUI_KEYFRAME_USE_REFERENCE`，重启服务 |
| `KEYFRAMES_NOT_CONFIRMED` | 仍有镜头没有确认关键帧 | 在关键帧审核里补齐 |
| `VISUAL_BIBLE_NOT_FOUND` | 项目没有视觉设定 | 先跑一次前期策划 |
| `REVISION_CONFLICT` | 视觉设定被其他页面改过 | 刷新页面后重新保存 |
| `FFMPEG_COMPOSE_FAILED` | 合成失败（常见于字体路径） | 检查 `FFMPEG_FONT_PATH` 是否指向存在的字体文件 |
| `COMPOSER_AUDIO_STREAM_MISSING` | 成片没有音轨 | 检查 TTS 音频资产是否存在且为 ready |

---

## 5. 想只验证“参考图是否真的进了关键帧”

不需要看整条视频，只要在关键帧审核里重新生成任意一个镜头的候选，然后运行：

```bash
node scripts/verify-accuracy.mjs --project=dd3c8fc7-f60b-4f1f-9c2b-91042d84d597 | Select-String "参考图"
```

或者直接查数据库：

```bash
node -e "const {DatabaseSync}=require('node:sqlite');const db=new DatabaseSync('data/wenying.sqlite');console.log(db.prepare(\"select id,status,json_extract(metadata_json,'$.manifestProfileId') profile,json_extract(metadata_json,'$.startImage') usedRef,created_at from media_assets where type='shot_keyframe_candidate' order by created_at desc limit 5\").all())"
```

新生成的候选应当是 `profile=krea2-keyframe-img2img`、`usedRef=1`。如果是 `krea2-turbo-t2i`、`usedRef=0`，说明参考图仍然没有被消费。

---

## 6. 视频和文案差距大时，按这个顺序调

不要先去调 steps、CFG 这类参数，它们改的是画质，不是"画得对不对"。按下面顺序排查：

1. **先看关键帧。** 关键帧错了，视频一定错。关键帧审核页可以直接改关键帧提示词后重新生成，成本远低于重跑视频。
2. **人物不像 / 每镜换脸。** 回视觉资产页改角色设定（年龄、面部、发型、服装），重新生成参考图并锁定，再重做关键帧；确认新关键帧的 `manifestProfileId` 是 `krea2-keyframe-img2img`。
3. **场景不对。** 改场景实体的空间描述、布局、光线、色板，重建场景母版。
4. **画面里出现了不该出现的东西。** 检查该镜头的 `mustNotShow` 是否包含它；编译出的负向提示会自动带上这些词。第 7 镜的列车、第 8/9 镜的女人都属于这一类。
5. **动作太少、太像静帧。** 运动提示由 `generationSpec.motionPrompt` 决定，I2V 只负责动作和运镜。注意：分镜一旦确认就不可原地改，需要从相机层重新生成一版策划，或者直接改关键帧让静态画面对得更准。
6. **只有上面都对了，再去调生成参数。** 此时调 steps、guidance、denoise、seed 才有意义。

已知限制：当前 I2V 每镜只能消费**一张**参考图，有角色的镜头优先用角色参考图，场景一致性靠关键帧提示词约束。等 14B 生产工作流接入后，再多参考图/控制图才是下一步。
