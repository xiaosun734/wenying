# 网文小说转视频工具 — 产品需求文档（PRD）

> 版本：v0.1（构思阶段）
> 日期：2026-09-04
> 文档状态：初稿，待评审

---

## 1. 产品概述

### 1.1 产品名称（暂定）
文影 · 网文转视频助手

### 1.2 一句话定位
面向自媒体博主的**网文小说一键转解说视频**工具，粘贴小说即可批量生成 3-5 分钟抖音/小红书风格讲小说视频。

### 1.3 核心价值
- **提效**：传统手动做一条讲小说视频需要 2-4 小时（找文案、配音、找画面、剪字幕），本工具压缩到 5-10 分钟。
- **批量**：支持一本小说多章节批量生成，博主可日产多条内容。
- **网文专属优化**：针对网文爽点结构、人物关系、情节节奏做专门的文案改写和画面匹配。
- **分段精修**：支持对单段内容独立调整（文案/配音/画面），无需整条视频重新生成。

---

## 2. 目标用户与使用场景

### 2.1 目标用户画像
| 维度 | 描述 |
|---|---|
| 身份 | 抖音/小红书/视频号自媒体博主，专注小说解说赛道 |
| 规模 | 个人博主或 2-3 人小团队 |
| 痛点 | 手动制作效率低、文案改写耗时、画面素材难找、人物形象不统一 |
| 付费意愿 | 愿意为提效工具支付月度会员费（参考剪映会员 25-30 元/月） |

### 2.2 典型使用场景
**场景 A：日常更新**
博主找到一本热门网文，复制链接粘贴到工具，选择"生成到第 10 章"，选择"悬疑"风格，点击批量生成，10 分钟后得到 3 条 3-5 分钟视频，导出后发布。

**场景 B：精修爆款**
博主对自动生成的某条视频中第 3 段不满意，在编辑器中单独修改该段解说文案、更换配音音色、重新生成该段画面，其他段落保持不变，5 分钟后导出最终版。

---

## 3. 功能需求

> 优先级标注：**P0 = MVP 必须** | P1 = 二期 | P2 = 三期/探索

### 3.1 内容输入模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 文本粘贴 | 用户直接粘贴小说正文文本 | P0 |
| 文件上传 | 支持上传 .txt 文件（后续拓展 .epub） | P1 |
| 链接抓取 | 粘贴小说网页链接，自动抓取正文内容（需处理反爬） | P1 |
| 章节识别 | 自动识别文本中的章节分隔（"第X章"等关键词），展示章节列表 | P1 |
| 范围选择 | 用户可选择生成到第几章、或指定生成总视频时长 | P1 |
| 题材选择 | 下拉选择小说题材（玄幻/都市/悬疑/言情/历史/科幻/其他），影响文案改写风格和画面风格 | P0 |

### 3.2 文本处理模块（AI 改写核心）

| 功能 | 描述 | 优先级 |
|---|---|---|
| 解说文案改写 | AI 将原文改写成适合口播的解说文案：压缩冗余、保留爽点、开头加钩子、结尾留悬念 | P0 |
| 自动分段 | 将改写后的文案按语义/场景切分为若干段，每段对应一个视频片段 | P0 |
| 人物/场景提取 | AI 自动提取文中出现的人物、场景、关键情节，以结构化列表展示 | P1 |
| 文案编辑 | 用户可在编辑器中直接修改任意一段的解说文案，修改后该段可独立重新配音+生成画面 | P0 |
| 改写风格调节 | 提供"快节奏/慢节奏""幽默/严肃"等风格选项，影响 AI 改写语气 | P1 |
| 批量文案预览 | 生成视频前，先展示完整改写文案供用户确认/修改，确认后再进入视频生成 | P0 |

### 3.3 视频生成模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 文生视频片段 | 每段解说文案对应生成一段 AI 视频（5-15 秒），画面内容匹配文案描述 | P0 |
| 人物一致性 | 同一角色在不同视频片段中保持长相、服装、发型一致（核心差异化功能） | P0 |
| 画面风格选择 | 用户选择整体视频风格（写实/动漫/国风/赛博朋克等），所有片段统一风格 | P0 |
| 单段重生成 | 对不满意的单个视频片段，可独立重新生成（支持修改提示词），不影响其他片段 | P0 |
| 片段替换 | 用户可上传自己的图片/视频替换某个 AI 生成片段 | P2 |
| 转场效果 | 片段之间自动添加转场（淡入淡出/滑动等），用户可选 | P1 |

### 3.4 配音模块（TTS）

| 功能 | 描述 | 优先级 |
|---|---|---|
| AI 语音合成 | 将解说文案合成为语音，支持多种音色 | P0 |
| 音色选择 | 提供男声/女声多种预设音色（沉稳男声/甜美女声/磁性旁白等） | P0 |
| 多角色配音 | 文中对话部分可自动识别角色，不同角色用不同音色朗读（需配合人物提取） | P1 |
| 情感语调 | 支持调节语速、语调、情感（激动/平静/紧张） | P1 |
| 单段重配音 | 修改某段文案后，仅重新合成该段语音 | P0 |
| 自定义音色克隆 | 用户上传音频样本，克隆自己的声音（高级会员功能） | P2 |

### 3.5 音频处理模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 自动配 BGM | 根据题材和情节自动匹配背景音乐，音量自动低于人声 | P0 |
| BGM 分段调整 | 用户可对某一段视频单独更换 BGM 或调节音量 | P0 |
| 用户上传 BGM | 支持上传自定义音乐文件 | P1 |
| 自动环境音效 | 根据文案内容自动添加环境音效（打斗声、脚步声、雨声、开门声等） | P0 |
| 音效库 | 提供常用音效素材库供用户手动添加 | P2 |

### 3.6 字幕模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 自动生成字幕 | 根据配音语音自动生成逐字字幕，时间轴对齐 | P0 |
| 字幕样式调节 | 字体、大小、颜色、描边、位置（底部/居中）可调 | P0 |
| 字幕编辑 | 用户可手动修改字幕文字、调整单条字幕时间 | P0 |
| 字幕模板 | 提供多种预设字幕样式模板（抖音爆款风/简约风/综艺风） | P1 |

### 3.7 视频编辑与预览模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 时间线编辑器 | 以片段为单位的时间线，展示每段的文案缩略、视频缩略、配音波形 | P0 |
| 片段拖拽排序 | 可拖拽调整片段顺序 | P1 |
| 片段删除/插入 | 删除某段或在两段之间插入新片段 | P1 |
| 实时预览 | 编辑器内可播放完整视频预览，支持逐段播放 | P0 |
| 软件内播放 | 生成完成后可在网页端直接播放完整视频 | P0 |

### 3.8 导出模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 视频导出 | 导出为 MP4 文件，分辨率可选 720p/1080p | P0 |
| 比例选择 | 导出比例可选 9:16（竖屏抖音）/ 16:9（横屏B站）/ 1:1 | P0 |
| 批量导出 | 批量生成的多条视频可打包下载（zip） | P1 |
| 直接发布 | 授权后直接发布到抖音/小红书（需平台 API 对接，难度高） | P2 |

### 3.9 用户与会员模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 注册登录 | 手机号/邮箱/微信登录 | P0 |
| 会员订阅 | 月度/年度会员，支持微信/支付宝支付 | P0 |
| 用量配额 | 免费用户每月有限生成次数，会员不限量或大幅提升 | P0 |
| 作品管理 | 用户的历史生成作品列表，可查看/编辑/删除/重新导出 | P0 |
| 作品云端存储 | 生成的视频在云端保存一定时长（会员 30 天/免费 7 天） | P1 |

### 3.10 批量生成模块

| 功能 | 描述 | 优先级 |
|---|---|---|
| 多章节批量 | 一次选择多个章节，自动生成多条独立视频 | P1 |
| 生成队列 | 批量任务进入队列，后台异步处理，用户可离开页面 | P1 |
| 进度通知 | 生成完成后站内通知/邮件通知 | P2 |

---

## 4. 核心用户流程

### 4.1 主流程（MVP）

```
用户登录
  ↓
选择输入方式（粘贴文本）
  ↓
粘贴小说正文 → 选择题材
  ↓
【AI 处理】改写解说文案 + 自动分段
  ↓
展示文案预览 → 用户可逐段修改
  ↓
确认文案 → 选择画面风格 + 配音音色
  ↓
【AI 处理】逐段生成视频片段 + 配音 + 字幕 + BGM
  ↓
进入编辑器 → 预览完整视频
  ↓
（可选）对不满意的段落单独修改/重生成
  ↓
选择导出比例和分辨率 → 导出 MP4
```

### 4.2 分段精修流程（差异化核心）

```
在编辑器中选中某一段
  ↓
修改文案 / 更换音色 / 修改画面提示词
  ↓
点击"仅重新生成本段"
  ↓
后台仅处理该段（配音+视频+字幕），其他段落冻结
  ↓
替换到时间线对应位置 → 重新预览
```

---

## 5. 技术架构建议

### 5.1 整体架构

```
┌─────────────────────────────────────────┐
│           前端 Vue3 + TypeScript         │
│  编辑器(时间线) / 预览播放器 / 文案编辑  │
└──────────────┬──────────────────────────┘
               │ HTTPS / WebSocket
┌──────────────▼──────────────────────────┐
│         后端 Node.js (NestJS)            │
│  API 网关 / 任务调度 / 用户管理 / 计费    │
└──────┬───────────┬───────────┬──────────┘
       │           │           │
  ┌────▼───┐  ┌────▼───┐  ┌───▼────┐
  │ MySQL  │  │ Redis  │  │ 对象存储│
  │业务数据│  │缓存/队列│  │(视频/图片)│
  └────────┘  └────────┘  └────────┘
       │
  ┌────▼──────────────────────────────────┐
  │        第三方 AI 服务层                 │
  │  文本大模型 / TTS / 文生视频 / 字幕识别 │
  └───────────────────────────────────────┘
```

### 5.2 技术栈选型

| 层级 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue3 + TypeScript + Vite | 用户已确定 |
| 前端状态 | Pinia | 管理编辑器状态、任务状态 |
| 视频编辑 | 自研轻量时间线 + Video.js / shaka-player | 不建议引入重量级 FFmpeg.wasm，剪辑逻辑在后端做 |
| 后端 | Node.js + NestJS + TypeScript | 用户已确定，模块化架构适合本项目 |
| 数据库 | MySQL 8.0 | 用户、作品、任务、计费数据 |
| 缓存/队列 | Redis + BullMQ | 视频生成任务队列、异步处理 |
| 对象存储 | 阿里云 OSS / 腾讯云 COS | 存储生成的视频、图片、音频 |
| 视频合成 | 后端 FFmpeg | 拼接片段、合成字幕、混音、导出 |
| 实时通信 | WebSocket (Socket.io) | 推送生成进度到前端 |

### 5.3 关键设计：异步任务流水线

视频生成是长耗时任务（一条 3-5 分钟视频可能需要 5-15 分钟），必须异步处理：

```
用户提交生成任务
  → 写入 MySQL（status: pending）
  → 推入 Redis 队列（BullMQ）
  → Worker 消费任务，按流水线逐步处理：
    Step 1: 文本改写（LLM API）
    Step 2: 分段 + 人物提取（LLM API）
    Step 3: 逐段 TTS 配音（并发调用 TTS API）
    Step 4: 逐段文生视频（并发调用视频 API，注意人物一致性）
    Step 5: 自动字幕（语音识别 / 或直接用文案时间轴）
    Step 6: BGM + 音效匹配
    Step 7: FFmpeg 合成最终视频
    Step 8: 上传 OSS，更新任务状态
  → 通过 WebSocket 实时推送每一步进度给前端
```

---

## 6. 第三方 API 选型建议

> 以下为国内可用方案，实际接入前需验证价格和效果。

### 6.1 文本大模型（文案改写 + 人物提取）

| 方案 | 优势 | 参考 |
|---|---|---|
| 豆包大模型（Doubao） | 国内访问稳定，中文网文理解好，价格低 | 火山引擎 |
| DeepSeek | 推理能力强，长文本处理好，价格极低 | deepseek.com |
| GPT-4o | 改写质量最高，但需海外代理 | OpenAI |

**建议 MVP 用豆包或 DeepSeek**，成本低、中文好。

### 6.2 TTS 语音合成

| 方案 | 优势 | 参考 |
|---|---|---|
| 豆包 TTS | 多音色、情感丰富、支持多角色，中文效果好 | 火山引擎语音合成 |
| 阿里云 CosyVoice | 支持音色克隆、情感控制 | 阿里云 |
| 微软 Azure TTS | 音色多、质量稳定，但国内访问需优化 | Azure |

**建议 MVP 用豆包 TTS**，和文本模型同一平台，对接方便。

### 6.3 文生视频（核心成本项）

| 方案 | 人物一致性 | 价格（参考） | 国内可用 |
|---|---|---|---|
| 即梦 AI（Jimeng） | 支持角色参考图，一致性较好 | 约 0.3-0.5 元/段 | ✅ |
| 可灵 AI（Kling） | 支持图片参考生成视频 | 约 0.3-0.6 元/段 | ✅ |
| 通义万相 | 阿里系，稳定性好 | 约 0.2-0.4 元/段 | ✅ |
| Runway / Pika | 效果最好但贵，国内需代理 | 约 1-3 元/段 | ❌ |

**建议 MVP 用即梦或可灵**，重点测试人物一致性效果。

### 6.4 人物一致性技术方案（核心难点）

这是本产品最关键的技术挑战，推荐方案：

```
1. 首次出现某角色时，用 LLM 提取角色外貌描述（年龄/发型/服装/特征）
2. 用文生图 API 生成该角色的"标准参考图"（正面清晰照）
3. 后续所有包含该角色的视频片段，都以这张参考图作为"角色参考"输入
4. 即梦/可灵均支持"参考图生成视频"功能，可保持角色一致性
5. 将角色参考图存入用户项目，同一本小说复用
```

**风险**：当前 AI 视频的人物一致性仍非 100% 完美，需要在产品中允许用户手动调整角色参考图，或对不满意的片段重生成。

### 6.5 字幕生成

- 方案 A：直接用改写文案 + TTS 返回的时间戳对齐，无需额外 ASR（成本最低，MVP 推荐）
- 方案 B：用语音识别 API（如豆包 ASR）对生成的配音做逐字识别，时间轴更精准

---

## 7. MVP 范围与迭代规划

### 7.1 MVP（第一版，目标：验证核心链路）

**必须实现：**
- 用户注册登录
- 粘贴文本输入
- 题材选择
- AI 改写解说文案 + 自动分段
- 文案预览与逐段编辑
- 选择画面风格 + 配音音色
- 逐段 AI 视频生成（含人物一致性基础方案）
- AI 配音合成
- 自动字幕（基础样式）
- 自动 BGM
- 视频预览播放
- 单段重新生成（文案/配音/画面）
- 导出 MP4（9:16 / 1080p）
- 会员订阅 + 用量配额
- 作品管理列表

**MVP 不做：**
- 链接抓取、文件上传
- 多角色配音
- 环境音效
- 批量生成
- 时间线拖拽排序
- 手机 APP

### 7.2 V1.1（二期，目标：提升体验和效率）

- 链接自动抓取 + 文件上传
- 章节识别 + 范围选择
- 人物/场景结构化提取与展示
- 多角色配音
- 环境音效自动添加
- BGM 分段调整 + 用户上传
- 字幕样式高级调节 + 模板
- 时间线拖拽/删除/插入
- 批量生成 + 队列

### 7.3 V2.0（三期，目标：差异化与高级功能）

- 音色克隆
- 自定义题材训练（用户可保存自己的改写风格）
- 角色管理（用户可手动设定角色外貌/参考图）
- 手机 APP（或 PWA）
- 直接发布到平台
- 社区模板分享（爆款文案模板、画面风格模板）

---

## 8. 核心技术难点与风险

| 难点 | 说明 | 应对策略 |
|---|---|---|
| **人物一致性** | AI 视频生成中同一角色跨片段保持一致是行业难题 | 用角色参考图方案 + 允许单段重生成 + 手动调整参考图 |
| **视频生成成本** | 一条 3-5 分钟视频约 20-30 个片段，每段 0.3-0.5 元，单条成本 6-15 元 | 会员定价覆盖成本；优化片段时长（减少片段数）；谈 API 批量折扣 |
| **生成耗时** | 单条视频可能 5-15 分钟，用户等待焦虑 | 异步队列 + 实时进度推送 + 文案预览确认（减少返工） |
| **文案改写质量** | AI 改写可能丢失爽点、逻辑不通 | 针对网文做 Prompt 优化；提供改写风格选项；允许用户逐段修改 |
| **链接抓取反爬** | 小说网站有反爬机制 | MVP 不做链接抓取；二期用无头浏览器 + 代理 IP 池 |
| **FFmpeg 合成稳定性** | 多片段拼接、字幕烧录、混音在高并发下可能出问题 | 任务队列限流 + 失败重试 + 详细日志 |

---

## 9. 非功能需求

### 9.1 性能
- 文案改写响应：≤ 30 秒（3000 字文本）
- 单条视频生成总时长：≤ 15 分钟（目标 10 分钟内）
- 视频预览加载：≤ 3 秒（CDN 加速）
- 并发任务：MVP 支持同时 50 个生成任务排队

### 9.2 成本控制（单条视频估算）
| 项目 | 估算成本 |
|---|---|
| 文本改写（LLM） | 0.05 - 0.15 元 |
| TTS 配音 | 0.10 - 0.30 元 |
| 视频生成（25 段 × 0.4 元） | 8 - 12 元 |
| 对象存储 + CDN | 0.10 - 0.30 元 |
| FFmpeg 计算（云服务器） | 0.20 - 0.50 元 |
| **合计** | **约 8.5 - 13.5 元/条** |

> 会员定价建议：月卡 39-59 元，每月 30 条额度；高级会员 99 元/月不限量。需根据实际 API 价格精算。

### 9.3 安全与合规
- 用户上传的小说内容仅用于生成，不存储原文（或加密存储）
- 生成视频的版权提示：提醒用户确保使用的小说内容有合法授权
- AI 生成内容标识：按国内法规要求，AI 生成视频需添加标识
- 数据加密：用户数据传输 HTTPS，敏感信息加密存储

---

## 10. 待确认事项

以下问题在进入开发前需要进一步明确：

1. **预算范围**：MVP 开发周期和预算大概多少？影响 API 选型和功能裁剪。
2. **目标上线平台**：是否需要备案？（国内网页端需要 ICP 备案）
3. **小说版权**：是否考虑与小说平台合作获取授权内容？还是纯工具定位（用户自负版权责任）？
4. **视频生成 API 实测**：建议在即梦和可灵各注册账号，用同一本小说测试 5-10 个片段，对比人物一致性效果和成本后再定。
5. **FFmpeg 部署**：视频合成是放在主后端服务器，还是单独的渲染 Worker 服务器？（建议单独，避免阻塞 API）

---

