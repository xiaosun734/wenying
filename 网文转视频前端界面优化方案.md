# 网文转视频前端界面优化方案

> 编制日期：2026-09-16  
> 适用项目：`C:\Users\m1738\Desktop\小说`  
> 参考方案：`C:\Users\m1738\Desktop\准确性优化\网文转视频准确性改造全流程方案.md`  
> 本文范围：只描述前端页面、交互、信息架构、状态展示和用户操作，不替代后端、模型、ComfyUI、音视频合成等流程方案。

---

## 1. 优化目标

当前前端已经具备“新建作品 → 文案处理 → 分镜确认 → 视频生成 → 编辑与导出”的基础页面，但关键的视觉资产和中间审核环节没有被独立呈现，用户很难知道：

- 小说中的角色到底对应哪一张生成图；
- 角色的正面图、侧面图、背面图和动作参考图是否已经准备好；
- 每个镜头使用了哪个角色版本、哪个场景版本；
- 关键帧是否已经确认，视频为什么不能生成；
- 生成失败是人物不一致、构图不对，还是出现了禁止元素；
- 修改角色外观后，哪些镜头需要重新确认。

本次前端优化的核心目标是：

1. **把“视觉资产确认”从文字配置中独立出来。**
2. **支持用户用文字描述创建角色正面图。**
3. **支持从已确认的正面图进入三视图和特殊动作参考图管理。**
4. **支持将文章角色与生成角色资产建立明确绑定。**
5. **在逐镜审核界面显示角色绑定、参考图和连续性约束。**
6. **在任何会造成返工的操作前，明确提示影响范围。**
7. **让用户始终知道当前项目处于哪个阶段、下一步该做什么。**

前端不应把“生成成功”简单等同于“内容正确”。界面要围绕“生成前确认、生成中可追踪、生成后可审核、失败后可定向处理”重新组织。

---

## 2. 当前前端与目标前端的差异

### 2.1 当前页面结构

现有前端主要通过 `app.js` 中的视图函数切换页面，包含：

- 工作台：`dashboard`
- 新建作品：`create`
- 文案处理和预览：`script-processing`、`script-preview`
- 配置和导演/分镜流程：`config`、`storyboard-processing`、`shot-review`
- 视频生成：`generation`
- 编辑与导出：`editor`
- 我的作品、会员中心和帮助页

当前工作流的主要问题是：用户确认分镜后，很快就进入视频生成，角色资产、场景母版、关键帧候选和逐镜视频候选没有形成完整的可视化审核链路。

### 2.2 目标页面结构

建议将创作流程调整为以下主导航或项目内步骤：

```text
工作台
  └─ 新建作品
      1. 文案与声音
      2. 角色与视觉资产
      3. 导演分析与结构化分镜
      4. 关键帧审核
      5. 视频候选审核
      6. 时间轴与导出
```

新增或重构的核心页面：

| 页面 | 作用 | 用户必须完成的事情 |
|---|---|---|
| 文案与声音 | 确认解说文案、配音和真实时长 | 确认文案版本和声音版本 |
| 角色与视觉资产 | 管理文章角色、角色正面图、三视图、动作参考图 | 至少确认主要角色的主参考资产 |
| 导演与结构化分镜 | 查看镜头意图、表达渠道和约束 | 确认镜头规格和角色绑定建议 |
| 关键帧审核 | 逐镜选择静态画面 | 每个正式镜头选择一个关键帧 |
| 视频候选审核 | 对生成视频进行逐镜审核 | 采用、定向重试或返回修改 |
| 时间轴与导出 | 查看真实时间轴、音视频轨道和导出检查 | 通过技术检查后导出 |

---

## 3. 全局交互原则

### 3.1 使用项目级阶段条

在新建作品后的所有页面顶部保留固定阶段条，建议显示：

```text
文案与声音 → 角色与视觉资产 → 导演与分镜 → 关键帧 → 视频候选 → 时间轴导出
```

每个阶段需要有三种状态：

- **已完成**：用户已确认，允许回看；
- **进行中**：当前需要处理；
- **待处理**：前置条件未完成，不允许直接进入；
- **需要关注**：存在 stale、失败、未绑定或未审核资产。

阶段条不只显示百分比，还要显示阻塞原因。例如：

> 关键帧阶段暂不能完成：还有 2 个镜头未选择角色参考图。

### 3.2 统一显示保存和版本状态

页面右上角建议增加项目状态栏：

- `已保存`
- `保存中`
- `有未保存修改`
- `存在旧版本素材`
- `当前版本已锁定`

所有视觉资产和审核结果都显示版本号，例如：

```text
沈砚 · 角色设定 v3 · 已锁定
废弃地铁站 · 场景母版 v2 · 需要重新确认
镜头 04 · 关键帧候选 v5 · 已选中
```

### 3.3 对破坏性操作提前告知影响范围

以下操作不能只弹一个“确定删除”提示，应明确列出影响范围：

- 更换角色主参考图；
- 修改角色年龄、发型、服装或脸型；
- 修改场景母版图；
- 解除文章角色与生成角色的绑定；
- 修改镜头中的角色绑定；
- 修改关键帧构图或动作；
- 重新生成某一镜头。

推荐提示样式：

> 更换“沈砚”的主参考图后，已使用旧参考图的 5 个关键帧将标记为“需要复核”。已导出视频不会被删除，但相关镜头不能继续使用旧资产生成新版本。是否继续？

### 3.4 重要状态不能只依赖颜色

状态标签同时使用：

- 文字；
- 图标；
- 颜色；
- 必要时的辅助说明。

例如不要只使用绿色边框表示“已确认”，应显示：`✓ 已确认`；不要只用红色表示失败，应显示：`! 语义审核失败：出现禁止角色`。

---

## 4. 页面一：文案与声音

该页面保留现有文案确认能力，但需要为后续角色和镜头资产提供明确的入口。

### 4.1 页面布局

建议采用三栏布局：

- 左侧：项目步骤和文案段落列表；
- 中间：文案编辑区；
- 右侧：声音、真实时长和进入下一阶段的检查项。

### 4.2 需要新增的内容

1. 在文案段落中标记自动识别到的角色名称。
2. 支持点击角色名称打开角色信息抽屉。
3. 在段落列表显示该段涉及的角色和场景标签。
4. 显示 TTS 实际时长，而不是只显示预估时长。
5. 语速修改后，提示用户后续镜头时间将重新计算。
6. 显示“文章角色清单”入口，进入角色与视觉资产页。

### 4.3 文案中的角色标注

角色标注不应改变文案内容，只作为前端辅助信息。例如：

```text
[沈砚] 在废弃的地铁站醒来。
广播里却没有报站名，只有一个沙哑的女声在叫他的名字。
```

建议使用轻量标签：

- `角色：沈砚`
- `角色：女孩`
- `场景：废弃地铁站`
- `道具：手机、照片、电子屏`

如果自动识别结果不准确，用户可以在右侧抽屉中修改名称、合并角色或标记为“非角色实体”。

### 4.4 进入下一步的检查

“进入角色与视觉资产”按钮旁边显示检查结果：

- 文案是否已确认；
- 是否存在未命名角色；
- 是否存在多个可能为同一角色的名称；
- 是否有关键角色尚未建立资产。

这些检查可以是警告，不必全部阻止进入；但如果文案版本没有确认，应阻止进入正式生产链路。

---

## 5. 页面二：角色与视觉资产中心

这是本次前端改造的核心页面。建议在项目内提供一个长期可进入的“角色与视觉资产”工作区，而不是只在首次生成时出现一次。

### 5.1 页面目标

该页面需要同时管理两类对象，但必须明确区分：

- **文章角色**：仅用于记录文案中出现的人物、声音或非实体角色；
- **生成角色**：完全由用户自行设定的视觉角色，不自动依赖文章内容生成。

用户可以在一个页面内完成：

1. 查看文章中识别出的角色；
2. 独立创建或编辑用户设定的生成角色；
3. 用用户输入的文字描述生成角色正面图；
4. 从确认的正面图生成固定三视图：正面、侧面、背面；
5. 提前批量生成并管理特殊动作参考图；
6. 选择主参考版本并锁定；
7. 将文章角色绑定到一个生成角色；
8. 查看哪些镜头正在使用该角色资产。

文章内容只用于识别文章角色、出镜关系和绑定入口，不应自动替用户决定生成角色的长相、服装或风格。

### 5.2 页面布局建议

采用“角色列表 + 资产工作区”布局：

```text
┌────────────────────────────────────────────────────────────┐
│ 项目步骤条                 保存状态       检查并进入下一步 │
├──────────────┬─────────────────────────────────────────────┤
│ 文章角色列表  │ 当前角色工作区                              │
│              │ 角色设定 / 正面图 / 三视图 / 特殊动作 / 使用情况 │
│ 沈砚 已绑定    │                                             │
│ 女孩 待确认    │                                             │
│ 广播女声 非人物 │                                             │
└──────────────┴─────────────────────────────────────────────┘
```

### 5.3 文章角色列表

左侧列表建议展示：

- 文章角色名称；
- 出现次数或涉及镜头数；
- 当前绑定状态；
- 主参考图缩略图；
- 资产完成度；
- 是否存在待审核问题。

状态示例：

| 状态 | 含义 |
|---|---|
| `待创建` | 文章中出现了角色，但尚未创建生成角色 |
| `待绑定` | 已有生成资产，但尚未与文章角色建立对应关系 |
| `已绑定` | 文章角色已关联一个生成角色 |
| `资产不完整` | 缺少正面图、三视图或必要动作参考图 |
| `需要复核` | 角色主参考图或设定发生变化 |
| `已锁定` | 当前版本可被镜头和关键帧引用 |

### 5.4 创建生成角色入口

生成角色必须由用户独立设定，不以文章内容作为外观生成依据。建议只提供一个主入口：

- `新建生成角色`：用户自行填写角色名称、外观、服装、风格和禁止项。

文章角色列表中的“创建角色”按钮可以作为快捷入口，但其作用只是打开空白的生成角色表单并在完成后进入绑定流程，不得自动把文章描述填入生成提示词。

创建后要明确区分以下信息：

- **用户设定**：生成角色的外观、服装、配饰、气质和视觉风格；
- **项目固定项**：用户确认后不应在不同镜头中随意变化的身份特征；
- **镜头状态**：某一镜头临时使用的姿势、表情、道具或动作；
- **文章绑定关系**：文章角色与生成角色的对应关系，不等同于生成角色的外观来源。

文章文本可以显示在绑定面板中作为上下文，但不应自动改写或补全用户的生成角色描述。

---

## 6. 角色正面图创建界面

### 6.1 核心交互

用户输入一段自然语言描述，点击“生成角色正面图”。建议表单包含：

1. **角色名称**：由用户自行设定，可在后续绑定到一个或多个文章角色；
2. **文字描述**：大文本框，完全由用户描述生成角色；
3. **外观约束**：年龄、性别呈现、脸型、发型、体型、肤色等；
4. **服装与配饰**：可输入服装、颜色、材质和固定配饰；
5. **视觉风格**：使用项目风格或单独设定角色风格；
6. **画面比例**：默认使用项目成片比例；
7. **禁止出现**：例如“不得出现其他人物、不得更换服装颜色”；
8. **可选参考图**：用户有草图或真人参考时可上传；
9. **生成额度提示**：明确提示本次角色正面图生成将消耗角色生成额度。

界面不应要求用户理解提示词工程。高级用户可以展开“高级描述”区域，但默认使用自然语言输入和结构化选项。

### 6.2 描述输入框设计

输入框下方提供可点击的用户设定模板：

```text
外观：年龄、脸型、发型、眼睛、体型、肤色
服装：上衣、下装、鞋、配饰、主色、材质
气质：沉稳、疲惫、警觉、冷漠等
画面：正面站立、全身、纯净背景、角色设定图
固定项：后续镜头中不得改变的外观特征
```

不要提供默认的“根据文章描述补全”操作。文章文本只作为角色绑定上下文，不作为生成角色的默认描述来源。用户可以完全从零创建一个与文章角色不同名的生成角色，再在绑定面板中进行对应。

### 6.3 候选图展示

每次角色正面图生成固定产生 4 张候选图，并在生成按钮旁显示本次消耗的角色生成额度。每张候选卡包含：

- 图片预览；
- 候选编号；
- 生成时间；
- 版本号；
- 质量检查状态；
- `设为正面主参考`；
- `放大查看`；
- `局部重绘`；
- `以此为基础重新生成`；
- `拒绝并说明原因`。

候选卡必须支持并排比较。比较模式中显示：

- 脸部一致性；
- 服装是否符合描述；
- 是否包含多余人物或道具；
- 是否适合后续生成三视图；
- 用户备注。

### 6.4 正面图确认

用户点击“设为正面主参考”后，弹出确认面板：

```text
你将把候选图 C-03 设为“沈砚”的正面主参考。
该资产将用于三视图、动作参考图和后续镜头绑定。
后续更换主参考图会影响已使用它的镜头。
```

确认后显示：

- `正面主参考 · v1`
- `已锁定`
- `被 0 个镜头使用`
- `继续生成三视图`

如果用户只想保存图片、不想作为主参考，应提供“仅保存为候选”选项，避免所有生成图都自动成为正式资产。

---

## 7. 三视图界面

### 7.1 进入方式

从正面主参考卡点击：`生成三视图`。

页面顶部固定显示主参考图和角色摘要，提醒用户：

> 三视图应保持与正面主参考相同的脸型、发型、服装、配饰和体型。若正面图本身不满意，请先返回修改主参考图。

### 7.2 展示方式

三视图固定为正面、侧面、背面，采用横向对照画板：

```text
┌────────────┬────────────┬────────────┐
│ 正面        │ 侧面        │ 背面        │
│ 预览        │ 预览        │ 预览        │
└────────────┴────────────┴────────────┘
```

三视图只包含正面、侧面和背面，不额外扩展视角。其他镜头角度通过镜头构图和动作状态管理，不新增三视图类型。

### 7.3 三视图审核项

用户可分别标记：

- 脸型一致；
- 发型一致；
- 服装一致；
- 配饰一致；
- 身材比例一致；
- 颜色一致；
- 是否出现多余人物或物件。

提供三种操作：

- `确认全部三视图`；
- `只重生成当前视角`；
- `返回更换正面主参考`。

### 7.4 版本和锁定

三视图需要独立记录视角版本，但在 UI 上让用户理解为一个角色资产包：

```text
角色资产包：沈砚 v1
  ├─ 正面主参考 v1 · 已确认
  ├─ 侧面参考 v1 · 已确认
  └─ 背面参考 v1 · 已确认
```

只有正面主参考确认后，三视图才允许进入“确认”；三视图未全部确认时，可以继续生成动作候选，但页面需要标记“角色资产包尚未完整”。

---

## 8. 特殊动作参考图界面

### 8.1 动作分类

特殊动作参考图采用提前批量生成的方式，不在每个镜头临时从零创建。用户可以在角色资产页一次性建立动作资产包，并为每个动作添加备注。

动作参考图不是简单的“再生成几张图片”，应按使用场景分类管理：

- 情绪：惊恐、警觉、悲伤、震惊、愤怒；
- 姿态：站立、坐下、跪地、奔跑、回头、跌倒；
- 叙事动作：拿手机、查看照片、推门、奔向隧道、抬头听声音；
- 镜头动作：近景表情、全身动作、背影、侧身、手部特写。

### 8.2 创建动作参考

表单建议包含：

- 动作名称；
- 文字描述；
- 情绪标签；
- 身体姿态；
- 手部动作；
- 道具绑定；
- 视角或景别；
- 是否允许改变服装和配饰；
- 是否作为通用动作资产保存。

示例：

```text
动作名称：听到隧道轰鸣后回头
描述：沈砚身体保持在画面右侧，突然转头望向左侧隧道，右手微微收紧，表情警觉。
道具：无
情绪：警觉
镜头参考：近景 / 半身 / 侧前方
服装变化：不允许
```

### 8.3 动作候选卡

动作候选卡显示：

- 动作参考图；
- 动作名称和标签；
- 人物资产版本；
- 是否符合动作描述；
- 可用于哪些镜头；
- 用户备注；
- 生成批次和角色生成额度消耗；
- `设为动作主参考`；
- `复制到其他角色`不建议默认提供，避免角色误用；
- `基于该姿态重新生成`。

建议动作资产包预置一组可编辑的常用动作，也允许用户批量添加自定义动作。动作生成完成后，用户应在动作卡中补充“适用场景”和“不要出现的变化”等备注。

### 8.4 动作图与镜头的关系

动作参考图可以被多个镜头引用，但前端要显示引用关系：

```text
“回头听声”动作参考 v1
已用于：镜头 05、镜头 07
```

若动作图的角色主参考版本发生变化，使用该动作图的镜头显示：`角色资产已更新，请重新确认`，而不是直接删除或隐藏旧图。

---

## 9. 文章角色与生成角色的绑定界面

这是保证“文章中的角色”和“生成出来的角色”不会混淆的关键功能。

### 9.1 绑定入口

建议在三个位置提供入口：

1. 角色与视觉资产页的文章角色列表；
2. 导演与结构化分镜页的角色列；
3. 关键帧审核页的镜头属性栏。

三处操作应修改同一份绑定关系，不要分别维护三套状态。

### 9.2 绑定面板

点击文章角色“女孩 · 待绑定”后打开右侧抽屉。文章角色信息只用于说明绑定对象和出镜上下文，不自动生成视觉设定：

```text
文章角色
女孩

文章上下文
- 三年前照片中的女孩
- 看不清脸
- 与沈砚有重要关系

选择用户已创建的生成角色
[角色卡：长发女性 v1]
[角色卡：灰色风衣女性 v2]
[+ 新建生成角色]

绑定规则
- 一个文章角色只能绑定一个生成角色
- 一个生成角色可以绑定多个文章角色
- 绑定默认对全项目生效

[确认全项目绑定]
```

### 9.3 绑定范围

绑定范围固定为：**全项目默认绑定**。

规则如下：

- 一个文章角色只能绑定一个生成角色；
- 一个生成角色可以被多个文章角色复用；
- 绑定确认后，文章角色在全项目中默认使用该生成角色；
- 角色的姿势、表情和动作变化通过动作参考图或镜头状态管理，不创建新的文章角色绑定；
- 当前阶段暂不处理角色资产变更后的自动失效传播，只在前端保留版本和使用情况展示。

绑定完成后，在镜头列表中显示：

```text
沈砚 → 全项目绑定：用户角色“沈砚 v3”
女孩 → 全项目绑定：用户角色“长发女性 v1”
```

### 9.4 绑定冲突提示

以下情况需要明确提示：

- 一个文章角色尝试绑定多个生成角色；
- 一个文章角色绑定的生成角色被删除或不可用；
- 镜头要求出现角色，但没有绑定任何资产；
- 一个生成角色被多个文章角色复用时，用户需要确认这种复用是有意的；
- 角色资产服装与镜头要求冲突；
- 角色被标记为“看不清脸”，但镜头选择了正脸近景；
- 文章角色在某段中是画外声音，不应自动生成实体人物。

提示应提供动作：

- `查看冲突镜头`；
- `保留当前绑定`；
- `仅对本镜头覆盖`；
- `返回角色资产页`。

### 9.5 绑定状态显示

在每个镜头和角色卡上使用一致的绑定徽标：

- `已绑定 · 全项目`
- `已绑定 · 本镜头覆盖`
- `未绑定`
- `绑定已过期`
- `角色不应出镜`

---

## 10. 页面三：导演与结构化分镜

原方案要求保留导演分析、RAG 证据和镜头选择，但前端需要让这些信息真正对用户可见，并形成可操作的“镜头规格卡”。

### 10.1 镜头规格卡

每个镜头卡建议显示：

- 镜头编号；
- 对应文案；
- 角色及其绑定资产；
- 场景及其参考资产；
- 景别、角度、焦段和构图；
- 起始动作；
- 表达渠道：画面、音频、字幕、后期；
- `must_show`：必须出现；
- `must_not_show`：禁止出现；
- 上一镜进入状态；
- 当前镜离开状态；
- 推荐生成方案及原因；
- 当前资产状态。

### 10.2 角色绑定列

不要只把角色名放在提示词文本中。建议在分镜表中增加“角色绑定”列：

| 角色 | 文章角色 | 生成资产 | 绑定范围 | 状态 |
|---|---|---|---|---|
| 沈砚 | 沈砚 | 沈砚 v3 | 全项目 | 已确认 |
| 女孩 | 女孩 | 女孩 v1 | 镜头 07 | 待确认 |

用户点击生成资产名称即可打开角色资产抽屉查看正面图、三视图和动作参考图。

### 10.3 允许人工覆盖，但不隐藏推荐原因

如果系统推荐“使用沈砚 v3 + 回头听声动作 v1”，用户可以覆盖，但应显示：

```text
推荐原因：该资产已在镜头 01、02、03 中确认，服装和发型一致。
```

人工覆盖后标记为：`人工覆盖`，方便后续排查。

---

## 11. 页面四：关键帧审核

### 11.1 页面布局

建议采用：

- 左侧：镜头列表和审核进度；
- 中间：关键帧候选图网格或大图对比；
- 右侧：镜头规格和角色/场景引用。

### 11.2 左侧镜头列表

每个镜头显示：

- 缩略图；
- 镜头编号和标题；
- 文案摘要；
- 角色绑定缩略图；
- 审核状态；
- 失败原因；
- 是否存在 stale 资产。

进度摘要示例：

```text
关键帧审核 5 / 7
已确认 4 · 待审核 1 · 需重生成 2
```

### 11.3 中间候选区

每个镜头固定展示 3～4 张候选，提供：

- 选择为正式关键帧；
- 放大查看；
- 并排比较；
- 局部重绘；
- 换构图；
- 换姿势；
- 重新生成；
- 查看生成参数和版本。

候选图上直接标注角色和场景参考：

```text
沈砚 v3 · 废弃地铁站 v2 · 回头听声 v1
```

### 11.4 右侧审核信息

右侧显示“本镜必须满足”：

- `必须出现`；
- `禁止出现`；
- `角色外观锁定项`；
- `场景布局锁定项`；
- `当前使用参考图`；
- `自动审核结果`；
- `用户备注`。

用户不需要打开 JSON 或完整提示词才能知道为什么这张图不合格。

### 11.5 确认按钮规则

“确认关键帧”按钮必须在以下条件满足时可用：

- 候选图已加载；
- 关键角色绑定明确；
- `must_show` 和 `must_not_show` 已展示；
- 没有未处理的阻断级审核失败。

如果只是轻微质量问题，允许用户勾选“接受风险并确认”，并要求填写备注。

### 11.6 批量操作

支持以下批量操作，但必须显示影响范围：

- 批量确认通过；
- 批量使用同一角色主参考；
- 批量重新生成失败镜头；
- 批量标记为需要人工审核。

禁止提供没有范围说明的“全部确认”。

---

## 12. 页面五：视频候选审核

### 12.1 页面结构

每个镜头进入视频候选审核后，建议显示：

- 视频播放器；
- 关键帧首帧对照；
- 动作描述；
- 角色绑定；
- 禁止内容；
- 自动审核报告；
- 候选版本切换；
- 采用、按原因重试、返回修改关键帧三个主操作。

### 12.2 审核报告可视化

审核报告不能只显示一个总分，应拆成：

- 人物身份一致性；
- 动作符合度；
- 场景一致性；
- 禁止元素检查；
- 画面稳定性；
- 时长和帧率检查；
- 音视频可用性。

每项显示：

```text
✓ 角色身份：通过
! 禁止元素：失败，检测到列车实体
✓ 场景布局：通过
```

### 12.3 定向重试入口

“重新生成”按钮应先让用户选择原因：

- 人物不像；
- 服装变化；
- 动作不对；
- 镜头运动不对；
- 出现禁止元素；
- 场景变形；
- 时长不合适；
- 其他。

选择原因后，前端展示将要保留和改变的内容。例如：

```text
本次重试将保留：关键帧、角色绑定、场景布局
本次重试将调整：动作描述、镜头运动
```

### 12.4 返回关键帧

当视频问题本质上来自静态构图时，提供明确的：

> 返回修改关键帧

并携带问题上下文，不要让用户回到镜头列表后重新寻找问题。

---

## 13. 页面六：时间轴与导出

### 13.1 时间轴显示真实时长

时间轴应以实际旁白音频和实际媒体时长为准，显示：

- 旁白轨；
- 视频轨；
- 音效轨；
- BGM 轨；
- 字幕轨；
- 图形文字/后期轨。

每个镜头显示：

- 起始时间；
- 结束时间；
- 真实时长；
- 是否 trim；
- 是否变速；
- 是否补帧；
- 对应视频版本。

### 13.2 技术检查面板

导出前显示检查结果：

- 是否存在未确认镜头；
- 是否存在未绑定角色；
- 是否存在 stale 素材；
- 是否存在视频缺少音轨；
- 是否存在音视频时长不一致；
- 是否存在字幕或图形文字待合成；
- 是否通过版权和 AI 生成标识检查。

将检查结果分为：

- `阻止导出`
- `建议处理`
- `已通过`

### 13.3 导出确认弹窗

保留现有比例和分辨率选择，同时新增“导出内容摘要”：

```text
本次导出
视频：7 个已确认镜头
旁白：已包含，真实时长 00:29.088
音效：2 条
字幕：已启用
图形文字：1 处（电子屏时间）
AI 生成标识：已启用
```

---

## 14. 角色资产数据在前端的最小字段

以下是前端为了正确展示和交互所需要的最小信息，不代表要求立即新增同名数据库表。

### 14.1 文章角色

```json
{
  "id": "story-character-1",
  "name": "沈砚",
  "sourceMentions": ["segment-1", "segment-2"],
  "sourceDescription": "刚参加过葬礼的男性",
  "bindingStatus": "bound",
  "boundVisualCharacterId": "visual-character-shenyan",
  "bindingScope": "project",
  "issues": []
}
```

### 14.2 生成角色

```json
{
  "id": "visual-character-shenyan",
  "name": "沈砚",
  "description": "28岁，清瘦长脸，短黑发，黑色单排扣西装",
  "sourceFacts": [],
  "productionDecisions": {
    "age": 28,
    "face": "清瘦长脸，单眼皮，眉骨清晰",
    "hair": "短黑发，额前少量碎发",
    "body": "约180cm，偏瘦",
    "costume": "黑色单排扣西装、白衬衫、无领带、黑色皮鞋"
  },
  "status": "locked",
  "currentVersion": 3,
  "referenceAssets": {
    "front": "asset-front-v1",
    "side": "asset-side-v1",
    "back": "asset-back-v1"
  },
  "actionAssets": ["action-turn-head-v1"],
  "usedByShots": ["shot-1", "shot-2"]
}
```

### 14.3 关键帧候选

```json
{
  "id": "keyframe-candidate-1",
  "shotId": "shot-5",
  "status": "selected",
  "assetUrl": "...",
  "characterBindings": [
    {
      "storyCharacterId": "story-character-1",
      "visualCharacterId": "visual-character-shenyan",
      "assetVersion": 3,
      "scope": "shot"
    }
  ],
  "referenceAssetIds": ["asset-front-v1", "action-turn-head-v1"],
  "mustShow": ["沈砚", "隧道", "转头反应"],
  "mustNotShow": ["列车实体", "女人", "其他乘客"],
  "qaStatus": "passed",
  "userNote": "构图和人物状态符合镜头要求"
}
```

### 14.4 前端展示所需的状态枚举

统一使用可读状态，不要在页面中直接展示内部英文值：

| 内部状态 | 前端文案 |
|---|---|
| `draft` | 草稿 |
| `candidate` | 候选 |
| `selected` | 已选中 |
| `confirmed` | 已确认 |
| `locked` | 已锁定 |
| `stale` | 需要复核 |
| `failed` | 生成失败 |
| `rejected` | 已拒绝 |
| `pending` | 等待处理 |

---

## 15. 与现有前端代码的落地建议

当前项目是原生 HTML + JavaScript + CSS 结构，建议先保持技术栈不变，采用增量改造，避免为了页面优化整体迁移框架。

### 15.1 `app.js` 视图层

建议新增或拆分以下视图函数：

```text
visual-assets             角色与视觉资产中心
character-detail          角色详情与正面图
character-turnaround      三视图审核
character-actions         特殊动作参考图
character-binding         文章角色绑定抽屉或弹窗
keyframe-review           关键帧审核
video-review              视频候选审核
export-check              导出前技术检查
```

现有 `shot-review` 可以保留，但建议增加角色绑定、`must_show`、`must_not_show` 和关键帧状态，不再只展示文字分镜。

### 15.2 `styles.css` 样式层

建议优先复用现有：

- `.panel`
- `.btn`
- `.status-pill`
- `.modal`
- `.timeline-*`
- `.property-*`
- `.processing-*`

新增样式类别：

- `.asset-library`
- `.character-card`
- `.asset-gallery`
- `.reference-strip`
- `.turnaround-board`
- `.action-reference-card`
- `.binding-drawer`
- `.review-checklist`
- `.stale-banner`
- `.impact-dialog`

### 15.3 不建议把所有逻辑放进提示词文本框

角色绑定、资产版本、审核状态和影响范围必须是结构化 UI 信息。提示词文本框只作为可读的生成描述或高级编辑入口，不能承担：

- 角色身份绑定；
- 资产版本控制；
- 镜头审核状态；
- 失败原因；
- 依赖关系。

---

## 16. 空状态、加载态和异常态

### 16.1 空状态

角色页无识别角色时：

> 暂未识别到明确角色。你可以从文案中手动添加角色，或直接创建一个生成角色。

未创建正面图时：

> 先用文字描述生成角色正面图，确认后再生成三视图和动作参考图。

### 16.2 加载态

生成候选图时，不要只显示一个全屏转圈。应显示：

- 当前角色名称；
- 当前操作：生成正面图 / 生成侧面图 / 生成动作图；
- 已完成候选数量；
- 预计下一步；
- 可否离开页面；
- 失败后的重试入口。

### 16.3 异常态

例如角色正面图生成失败：

```text
正面图生成失败
已保留你的文字描述，未修改现有主参考图。
[重试] [修改描述] [返回角色列表]
```

任何新版本生成失败，都不能覆盖已经确认的旧版本。

---

## 17. 权限、可访问性和易用性要求

### 17.1 易用性

- 主要按钮使用动词：`生成正面图`、`确认主参考`、`生成三视图`、`绑定角色`；
- 不使用只对内部开发者有意义的按钮文案；
- 所有图片卡片支持放大和键盘切换；
- 角色资产工作区支持返回，不丢失未保存文本；
- 生成任务在页面切换后仍能在项目状态中查看。

### 17.2 可访问性

- 图片候选卡提供角色名、版本和状态的文字替代信息；
- 颜色不是唯一状态依据；
- 弹窗打开后焦点进入弹窗，关闭后回到触发按钮；
- 预览视频提供暂停和静音控制；
- 文案、角色描述、备注输入框支持明确 label；
- 重要错误使用 `aria-live` 或等效提示，避免只显示 Toast 后消失。

### 17.3 响应式

优先保证桌面端三栏布局，同时为较窄窗口提供降级：

- 左侧角色列表可折叠；
- 中间画廊变为两列；
- 右侧属性面板变为底部抽屉；
- 视频审核中的对照面板可以切换标签页。

---

## 18. 前端验收标准

### 18.1 角色资产

- 用户可以从文章角色列表进入角色创建界面；
- 用户可以用文字描述生成角色正面图候选；
- 用户可以选择一张正面图作为主参考，并看到版本和锁定状态；
- 用户可以从正面主参考进入三视图界面；
- 三视图可以单独审核、重生成和确认；
- 用户可以创建并管理特殊动作参考图；
- 已确认角色资产不会被失败的新版本覆盖。

### 18.2 角色绑定

- 用户可以将文章角色绑定到生成角色；
- 用户可以选择全项目绑定或仅本镜头绑定；
- 分镜、关键帧和视频审核页显示一致的绑定关系；
- 未绑定角色或绑定过期时，页面显示明确警告；
- 用户可以从镜头直接跳转到角色资产页处理问题。

### 18.3 审核链路

- 用户可以查看每镜 3～4 张关键帧候选；
- 用户可以看到 `must_show` 和 `must_not_show`；
- 用户可以选择关键帧、重生成、换构图、换姿势或返回修改角色资产；
- 关键帧未确认时，页面明确显示为什么不能进入视频候选阶段；
- 视频候选页支持采用、按原因重试、返回关键帧三个动作；
- 导出前可以看到阻止导出的检查项。

### 18.4 一致性和回滚

- 修改角色主参考图会显示影响镜头范围；
- 旧版本资产保留在历史版本中；
- 页面可区分“未保存”“候选”“已确认”“需要复核”和“已锁定”；
- 刷新页面或离开页面后，任务和审核状态可以恢复。

---

## 19. 推荐实施顺序

### 第一阶段：先建立角色资产中心

1. 增加项目内“角色与视觉资产”入口，并显示角色生成额度；
2. 增加文章角色列表；
3. 增加角色描述表单和正面图候选卡；
4. 增加正面主参考确认和版本状态；
5. 增加基础角色详情页。

### 第二阶段：补齐三视图和动作参考图

1. 增加三视图画板；
2. 增加单视角重新生成；
3. 增加动作参考图分类和候选管理；
4. 增加资产引用镜头列表；
5. 增加角色资产变更影响提示。

### 第三阶段：接入文章角色绑定

1. 增加全项目绑定；
2. 增加单镜头覆盖绑定；
3. 在分镜页显示角色资产；
4. 在关键帧页显示角色参考图；
5. 增加未绑定、冲突和过期状态。

### 第四阶段：完善关键帧和视频审核

1. 将关键帧候选从视频生成流程中独立展示；
2. 增加逐镜审核清单；
3. 增加视频候选对照审核；
4. 增加按失败原因重试的 UI；
5. 增加时间轴和导出前检查。

---

## 20. 明确不在本文中决定的内容

以下内容属于视频生成或媒体处理流程，本文只要求前端能够展示状态或提供入口，具体方案需另行审核：

- 正面图、三视图和动作图实际使用哪一种模型或工作流；
- 三视图和动作图是否批量生成、生成几张候选；
- 角色参考图如何注入 T2I/I2V 工作流；
- 如何保证 I2V 实际消费首帧；
- 如何保存 seed、工作流版本和生成参数；
- 视频时长、帧数、TTS 实际时长和 FFmpeg 合成逻辑；
- 画外事件如何通过音频、字幕或后期表达；
- 自动审核模型、OCR、VLM 和定向重试策略；
- 资产 stale 的后端失效传播和数据库实现。

前端需要为上述能力预留状态展示和操作入口，但不应在界面层自行假设实现细节。

---

## 21. 结论

前端优化的重点不是增加更多按钮，而是把原本隐藏在提示词、数据库和生成任务中的“视觉决策”显性化：

1. 用户先创建并确认角色视觉资产；
2. 再查看正面图、三视图和动作参考图；
3. 将文章角色与生成角色明确绑定；
4. 在每个镜头中看到具体使用的角色资产；
5. 先审核关键帧，再审核视频候选；
6. 最后在真实时间轴上完成导出检查。

这样可以让用户在最便宜、最容易修改的阶段发现人物外观和构图问题，避免等到视频生成完成后才发现角色已经发生漂移。






